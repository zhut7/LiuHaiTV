# 注意：.gitignore **不支持行尾注释** —— 「模式 # 注释」会被当成一个含空格和 # 的
# 完整模式，结果一条也匹配不上。所以本文件的注释一律单独占一行。

# ---------------------------------------------------------------------------
# 运行期生成物 —— 里面有你自己同步来的第三方直播源地址，不该进公共仓库
# ---------------------------------------------------------------------------
data/
logs/
__pycache__/
*.py[cod]
*.egg-info/

# ---------------------------------------------------------------------------
# 打包产物 / 中间文件 —— 体积大且随时可重建
# ---------------------------------------------------------------------------
build/
dist/
protable/
# 成品压的包；要分发给别人请走 GitHub Release，别塞进仓库
*.rar
*.zip
*.7z
*.spec.bak
*.manifest

# ---------------------------------------------------------------------------
# 播放内核 libmpv-2.dll（约 115 MB）
# 超过 GitHub「单文件 100 MB」硬上限，提交会被拒绝；用 Git LFS 又要占
# 每月 1 GiB 下载带宽。所以每个使用者各自下载一次放进 bin/，仓库里不放。
# 下载地址与注意事项见 README 的「准备 libmpv-2.dll」一节。
# ---------------------------------------------------------------------------
bin/

# ---------------------------------------------------------------------------
# 本地工具 / 编辑器
# ---------------------------------------------------------------------------
# WorkBuddy 的项目记忆与会话记录
.workbuddy/
.claudeignore
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db
